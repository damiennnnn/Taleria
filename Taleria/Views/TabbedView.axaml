<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:Taleria.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             xmlns:views="using:Taleria.Views"
             xmlns:iconPacks="https://github.com/MahApps/IconPacks.Avalonia"
             x:DataType="vm:TabbedViewModel"
             x:Class="Taleria.Views.TabbedView">
    <UserControl.Styles>
        <Style Selector="iconPacks|PackIconCoolicons.LeftArrow">
            <Setter Property="Kind" Value="{x:Static iconPacks:PackIconCooliconsKind.ArrowLeftMd}"/>
        </Style>
        <Style Selector="iconPacks|PackIconCoolicons.RightArrow">
            <Setter Property="Kind" Value="{x:Static iconPacks:PackIconCooliconsKind.ArrowRightMd}"/>
        </Style>
    </UserControl.Styles>
    <SplitView Name="TabbedSplitView"
               DisplayMode="CompactOverlay"
               PanePlacement="Left">
        <SplitView.Pane>
            <Grid RowDefinitions="Auto,*">
                <ToggleButton Theme="{DynamicResource OutlineButton}"
                              Margin="8"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              Cursor="Arrow"
                              IsChecked="{Binding #TabbedSplitView.IsPaneOpen, Mode=TwoWay}">
                    <iconPacks:PackIconCoolicons
                        Classes.RightArrow="{Binding !#TabbedSplitView.IsPaneOpen}"
                        Classes.LeftArrow="{Binding #TabbedSplitView.IsPaneOpen}"
                        Height="16"
                        Width="16" />
                </ToggleButton>
                <ListBox Grid.Row="1"
                         Margin="2">
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Height="20">
                            <iconPacks:PackIconCoolicons Data="{iconPacks:Coolicons ArrowRightMd}"
                                      VerticalAlignment="Center"
                                      Height="18"
                                      Width="18" />

                            <Label Content="API Client"
                                   VerticalAlignment="Center"
                                   Margin="16,0,0,0"
                                   FontSize="18" />
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>
            </Grid>
        </SplitView.Pane>

        <views:MainView DataContext="{Binding MainViewModel}" />
    </SplitView>
</UserControl>